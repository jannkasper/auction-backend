server:
  port: 9001

spring:
  application:
    name: DEPARTMENT-SERVICE
  cloud:
    config:
      import-check:
        enabled: false

#eureka:
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#  instance:
#    hostname: localhost
